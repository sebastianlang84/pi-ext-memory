{
  "name": "pi-memory",
  "version": "2.1.4",
  "description": "A lightweight local memory system for Pi coding agent.",
  "keywords": [
    "pi-package"
  ],
  "type": "module",
  "peerDependencies": {
    "@mariozechner/pi-ai": "*",
    "@mariozechner/pi-coding-agent": "*",
    "typebox": "*"
  },
  "pi": {
    "extensions": [
      "./src/pi-extension/index.ts"
    ]
  },
  "scripts": {
    "test": "node --experimental-strip-types --test",
    "check:token-injection": "node --disable-warning=ExperimentalWarning --experimental-strip-types scripts/check-token-injection.ts",
    "smoke:memory-status": "pi -p \"/memory-status\"",
    "smoke:package-status": "pi --no-extensions -e . -p \"/memory-status\""
  }
}
